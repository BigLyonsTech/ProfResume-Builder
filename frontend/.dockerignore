# Frontend .dockerignore
# node_modules is huge — always exclude it from Docker build context
# Docker will run npm install fresh inside the container

node_modules/
dist/
.vite/
*.log
.git/
.gitignore
README.md
